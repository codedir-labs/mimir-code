# Test Fixtures

This directory contains test data and mock responses for testing.

## Structure

- `mock-responses/` - Mock HTTP responses from LLM providers
- `sample-configs/` - Sample configuration files
- `sample-projects/` - Sample project structures for integration tests
