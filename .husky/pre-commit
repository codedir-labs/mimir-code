# Fast checks for commits - staged files only
yarn lint-staged

# Quick typecheck (incremental, fast)
echo "📝 Running typecheck..."
yarn typecheck || {
  echo "❌ TypeScript errors found - commit blocked"
  exit 1
}
echo "✅ Typecheck passed"
